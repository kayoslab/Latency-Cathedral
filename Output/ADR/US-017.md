No ADR required. This ticket adds a single new Playwright spec file (`tests/e2e/smoke.spec.ts`) that consolidates existing acceptance criteria into an integrated smoke test. No new dependencies, no architectural changes, no new browser APIs, no changes to domain/metrics/render layers. The plan correctly identifies that 5 existing e2e specs already cover the individual acceptance criteria and adds value by testing the integrated preset→render flow (a gap where presets and rendering are currently tested separately). Playwright and its Chromium binary are already declared dependencies. The `test:e2e` npm script already exists. Estimated complexity of "low" is accurate.
