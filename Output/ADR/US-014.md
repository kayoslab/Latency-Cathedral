No ADR required. US-014 adds a debug HUD overlay, a keyboard toggle, and a `QualityBand` derived type — all following established patterns already present in the codebase. Specifically: (1) the HUD follows the same inline-style DOM creation pattern as `presetSelector.ts`, attaching to the existing `overlay` div, (2) the keyboard toggle follows the disposable-subscription pattern from `visibilityManager.ts`, (3) `QualityBand` is a pure domain derivation consistent with existing types in `src/domain/types.ts`, and (4) wiring into `main.ts` uses the established `aggregator.subscribe()` callback. No new dependencies, no new browser APIs beyond `keydown` (standard DOM event), no architectural departures, no framework additions. The plan's 10 steps are well-scoped and correctly sequenced.

**Validation notes:**

- **Plan step 1 (QualityBand type):** Confirmed `src/domain/types.ts` has no quality band concept — addition is necessary and appropriate in domain layer.
- **Plan step 3 (debugHud.ts):** The overlay div (`position: fixed, pointer-events: none, z-index: 1`) is the correct attachment point. HUD should set `pointer-events: none` on its own container to avoid intercepting clicks. Starting hidden by default is correct for a developer tool.
- **Plan step 5 (main.ts wiring):** The `aggregator.subscribe` callback at line 43 is the natural integration point. The plan correctly identifies that `mapSnapshotToScene` is already called there, so `sceneParams` are available to pass to the HUD.
- **Plan risk re: vitest environment:** Confirmed `vitest.config.ts` uses `environment: 'node'`. The plan correctly recommends per-file `// @vitest-environment jsdom` directives for DOM tests rather than changing global config.
- **Plan risk re: aggregator cadence:** 2s update frequency is appropriate for a debug HUD. No need for requestAnimationFrame hookup.
- **shell.ts resize concern from prior context is inapplicable:** `shell.ts` (lines 1-20) contains no resize handler — it only locates DOM elements. The earlier architectural note about shell resize coordination does not apply to this ticket.
