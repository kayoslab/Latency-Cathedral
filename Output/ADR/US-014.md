No ADR required. The debug HUD adds pure domain functions (`normalizeLoss`, `computeDegradation`, `deriveQualityBand`) to existing `src/domain/` modules and a new `src/ui/debugHud.ts` component — all following established repository conventions. The `computeDegradation` extraction from `snapshotToScene.ts` is a mechanical refactor with no behavior change. The class-based DebugHud pattern (vs functional `initPresetSelector`) is justified by the component's mutable visibility state and repeated update cycle. No new dependencies, no framework additions, no architectural departures from CLAUDE.md conventions.
