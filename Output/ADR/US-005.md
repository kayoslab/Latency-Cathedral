No ADR required. This ticket adds pure TypeScript interfaces and normalization helpers to the empty `src/domain/` directory — the exact location designated for domain models in the established project conventions (CLAUDE.md, ADR-004). No new dependencies, no architectural departures, no framework additions. The domain purity constraint (no three.js imports in `src/domain/`) is enforced by convention and can be verified via typecheck. The plan is additive, low-risk, and fully aligned with the existing architecture.
