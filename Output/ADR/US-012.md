No ADR required. The ruin modifiers module (`src/render/ruinModifiers.ts`) follows established patterns in the render layer, consumes existing SceneParams fields (`fracture`, `ruinLevel`) already mapped in `snapshotToScene.ts`, and uses three.js primitives (BoxGeometry, MeshStandardMaterial) consistent with the existing cathedral geometry builder. ADR-004 (three.js renderer bootstrap) covers the rendering architecture. The builder's own doc comment explicitly defers these parameters to US-012. No new dependencies, no new browser APIs, no structural departures.
